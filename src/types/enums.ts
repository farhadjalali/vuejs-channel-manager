export enum ChannelType {
    Phone = 1,
    Whatsapp = 2,
    Gmail = 3,
}

export enum FontAwesomeType {
    Solid = "fas",
    Regular = 'far',
    Light = 'fal',
    Duotone = 'fad',
    Brands = 'fab',
}
